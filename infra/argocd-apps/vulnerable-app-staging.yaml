apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vulnerable-app-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Scarfacemoignon/hackathon_ovh_team7.git
    targetRevision: main
    path: apps/vulnerable-app/staging
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    # Pas de "automated" : promotion manuelle uniquement, apres validation complete de dev.
    syncOptions:
      - CreateNamespace=true
